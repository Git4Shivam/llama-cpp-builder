name: Build No-AVX Wheel
on: workflow_dispatch
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Upgrade pip and core build tools
        run: python -m pip install --upgrade pip setuptools wheel build

      - name: Setup CUDA 12.1
        uses: Jimver/cuda-toolkit@v0.2.17
        id: cuda-toolkit
        with:
          cuda: '12.1.0'

      - name: Setup MSVC Compiler environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Set CUDA environment variables (safe append)
        shell: pwsh
        run: |
          $cudaPath = "${{ steps.cuda-toolkit.outputs.CUDA_PATH }}"
          if (-not $cudaPath) {
            $cudaPath = "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.1"
          }
          if (-not (Test-Path $cudaPath)) {
            throw "CUDA_PATH '$cudaPath' does not exist — CUDA toolkit setup may have failed."
          }
          Write-Host "Using CUDA_PATH: $cudaPath"
          Add-Content -Path $env:GITHUB_ENV -Value "CUDA_PATH=$cudaPath"
          Add-Content -Path $env:GITHUB_ENV -Value "CUDA_HOME=$cudaPath"
          Add-Content -Path $env:GITHUB_PATH -Value "$cudaPath\bin"

      - name: Verify nvcc and environment
        shell: pwsh
        run: |
          Write-Host "CUDA_PATH=$env:CUDA_PATH"
          Write-Host "PATH (first 200 chars): $($env:PATH.Substring(0,[math]::Min(200,$env:PATH.Length)))"
          nvcc --version

      - name: Clean CMake cache
        shell: pwsh
        run: Remove-Item -Recurse -Force -ErrorAction SilentlyContinue "build"

      - name: Install build dependencies
        run: python -m pip install wheel setuptools cmake scikit-build-core ninja

      - name: Show CMake & MSVC versions
        shell: pwsh
        run: |
          cmake --version
          $clPath = Get-Command cl.exe -ErrorAction SilentlyContinue
          if (-not $clPath) {
            throw "cl.exe not found on PATH — MSVC environment setup failed."
          }
          Write-Host "Found cl.exe at: $($clPath.Source)"

      - name: Compile Wheel (No AVX) and build with CUDA
        shell: pwsh
        env:
          # RTX 3060 = Ampere = compute capability 86. Trimmed from the
          # original multi-arch list (70;75;80;86) to cut build time,
          # since this wheel only needs to run on a 3060.
          CMAKE_ARGS: "-DGGML_AVX2=OFF -DGGML_AVX=OFF -DGGML_F16C=OFF -DGGML_FMA=OFF -DGGML_CUDA=ON -DGGML_CUDA_ARCHITECTURES=86"
          FORCE_CMAKE: "1"
          CUDACXX: "${{ steps.cuda-toolkit.outputs.CUDA_PATH }}\\bin\\nvcc.exe"
        run: |
          # --no-deps so pip doesn't also build wheels for every dependency
          # (numpy, diskcache, etc.) — keeps the artifact upload clean.
          python -m pip wheel llama-cpp-python==0.3.20 --no-deps --verbose

      - name: List created artifacts
        shell: pwsh
        run: |
          Write-Host "Created wheel files:"
          $wheels = Get-ChildItem -Name "llama_cpp_python-*.whl" -ErrorAction SilentlyContinue
          if (-not $wheels) {
            throw "No llama_cpp_python wheel found — build likely failed."
          }
          $wheels

      - name: Upload Finished Wheel
        uses: actions/upload-artifact@v4
        with:
          name: custom-llama-wheel
          path: "llama_cpp_python-*.whl"
